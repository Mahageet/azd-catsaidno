{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "environmentName": {
      "value": "${{ env.environment }}"
    },
    "location": {
      "value": "eastus"
    },
    "principalId": {
      "value": "${AZURE_PRINCIPAL_ID}"
    },
    "owner":{
      "value": "${AZURE_PROJECT_OWNER}"
    }
  }
}